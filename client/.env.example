# API base URL (leave empty in dev — Vite proxy handles /api)
VITE_API_URL=

# Couple names displayed on upload page
VITE_COUPLE_NAME=Sarah & James

# Wedding date subtitle (optional)
VITE_WEDDING_DATE=June 22, 2026

# Admin password (client-side gate until backend auth is added)
VITE_ADMIN_PASSWORD=change-me-in-production
